(() => {
  "use strict";
  // HUB Verify v0.2.1 compatibility shim.
  // The extension is being converted into a global lobby relay and must no
  // longer bind observed /register codes to the currently open HUB account.
  // Intentionally no UI or verification side effects here.
})();
